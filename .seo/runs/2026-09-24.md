# 24 September 2026 — first measurement and repair

Source: public Initial Commit SEO workflow preview. Full skill unavailable behind membership. This run implements the public measure/select/repair/verify/register pattern without claiming to run the private scripts.

## Baseline vitals

- Speed: ten sitemap URLs sampled once; maximum observed TTFB 310 ms; total uncompressed HTML 280,460 bytes. Network-inclusive observations, not field performance or CWV.
- Relevance: keyword intent and audience not validated against search query data.
- Google CTR: unavailable; Search Console not connected.
- Link health: zero broken internal links, zero redirecting internal links, zero orphan pages, and all sitemap URLs reachable from home in server HTML.

## Candidates and decision

1. **Selected: repair missing page headings.** Seven catalog/stream pages lack an h1; directly observed in returned HTML, shared component fix, small risk. Preserve appearance and keep the episode's existing h1 as its sole page heading.
2. Improve query-specific titles and snippets: defer until impressions and query intent are available; current evidence cannot establish a CTR problem.
3. Create new landing pages or editorial content: defer; no demand evidence or approved editorial brief.
4. Change homepage canonical: rejected; the root URLs with and without the trailing slash are equivalent. Correct the measurement instead.

A semantic heading improvement does not establish a ranking or traffic gain. Recheck through Search Console after enough observations accumulate.

## OpenSEO assessment

The second user link refers to OpenSEO (https://github.com/every-app/open-seo). It can supply data through MCP and supports Google Search Console integration. It complements an action-selection workflow. Its MIT code is available for self-hosting; DataForSEO usage and hosting may cost money. No OpenSEO installation or paid API calls were performed.

## Verification

Application TypeScript and OpenNext build passed. The built local Worker was audited across all ten sitemap pages: exactly one h1 on each page, matching normalized canonicals, present descriptions, and zero broken links or orphans. See `candidate.json`. Local timing (maximum 564 ms) is not comparable to the production baseline; these are different environments. A direct isolated component-render probe could not run without Next router context once concurrent QuickSearch edits landed; the full Worker HTTP audit verified the rendered result instead. Changes are local and not deployed or pushed in this pass, because the checkout also contains concurrent search and styling work. No outreach, backlink purchases, publishing quotas, unsupported product claims or recurring jobs were introduced.
